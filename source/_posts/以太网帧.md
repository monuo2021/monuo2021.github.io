---
title: 以太网帧
mathjax: true
date: 2022-01-18 09:40:43
tags:
categories: 计算机网络
description: 以太网帧是数据链路层协议数据单元，使用底层以太网物理层传输机制。换句话说，以太网链路上的数据单元将以太网帧作为其有效载荷(payload)进行传输。
---

**以太网帧**是数据链路层协议数据单元，使用底层以太网物理层传输机制。换句话说，以太网链路上的数据单元将以太网帧作为其有效载荷(payload)进行传输。

## 结构

以太网帧前面有**前同步码(preamble)**和**帧开始定界符 (SFD)**，它们都是物理层以太网数据包的一部分。每个以太网帧都以**以太网报头**开始，其中包含目标和源 MAC 地址作为其前两个字段。帧的中间部分是**有效载荷数据(payload)**，包括帧中携带的其他协议（例如 Internet 协议）的首部。帧以**帧校验序列(FCS)**结束，FCS 是一种 32 位循环冗余校验(CRC)，用于检测任何传输中的数据损坏。

![IEEE 802.3以太网数据包和帧结构](https://gitee.com/tanliang-TL/blogpic/raw/master/img/image-20211002095359578.png)

| 字段                      | 含义                                                                                                                                                                                                                                     |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 前同步码                  | 用来使接收端的适配器在接收 MAC 帧时能够迅速调整时钟频率，使它和发送端的频率相同。前同步码为 7 个字节，1 和 0 交替。                                                                                                                      |
| 帧开始定界符（SFD）       | 帧的起始符，为 1 个字节。前 6 位 1 和 0 交替，最后的两个连续的 1 表示告诉接收端适配器：“帧信息要来了，准备接收”。                                                                                                                        |
| 目的地址                  | 接收端的物理地址（MAC 地址），为 6 个字节（48 比特）。                                                                                                                                                                                   |
| 源地址                    | 发送端的物理地址（MAC 地址），为 6 个字节（48 比特）。                                                                                                                                                                                   |
| 类型                      | 上层协议的类型。由于上层协议众多，所以在处理数据的时候必须设置该字段，标识数据交付哪个协议处理。例如，字段为 0x0800 时，表示将数据交付给 IP 协议。                                                                                       |
| 数据                      | 也称为效载荷，表示交付给上层的数据。以太网帧数据长度最小为 46 字节，最大为 1500 字节。如果不足 46 字节时，会填充到最小长度。最大值也叫最大传输单元（MTU）。                                                                              |
| 帧检验序列 FCS            | 检测该帧是否出现差错，占 4 个字节（32 比特）。发送端计算帧的循环冗余码校验（CRC）值，把这个值写到帧里。接收端重新计算 CRC，与 FCS 字段的值进行比较。如果两个值不相同，则表示传输过程中发生了数据丢失或改变。这时，就需要重新传输这一帧。 |
| 包间间隙(Interpacket gap) | 包间间隙(IPG) 是包之间的空闲时间。发送一个数据包后，发送器需要在发送下一个数据包之前传输最少 96 位（12 个八位字节）的空闲线路状态。                                                                                                      |

## 类型

- Ethernet II
- Novell raw IEEE 802.3
- IEEE 802.2 Logical Link Control (LLC)
- IEEE 802.2 Subnetwork Access Protocol (SNAP)

不同的帧类型具有不同的格式和 MTU 值，但可以共存于同一物理介质上。根据下表可以区分帧类型。

此外，所有四种以太网帧类型都可以选择包含一个 IEEE 802.1Q 标记，以标识它属于哪个 VLAN 及其优先级（服务质量）。这种封装在 IEEE 802.3ac 规范中定义，并将最大帧增加 4 个八位字节。

<img src="C:\Users\谭梁\AppData\Roaming\Typora\typora-user-images\image-20211002104229791.png" alt="image-20211002104229791" style="zoom: 80%;" />

**Ethernet II 帧格式**和**IEEE 802.3 格式**，这两种格式的主要区别在于，Ethernet II 格式中包含一个**Type 字段**，标识以太帧处理完成之后将被发送到哪个上层协议进行处理。IEEE802.3 格式中，同样的位置是**长度字段(Length)**。不同的**Type 字段值/Length 字段**可以用来区别这两种帧的类型，当该字段小于等于 1500（或者十六进制的 0x05DC）时，帧使用的是 IEEE 802.3 格式。当该字段值大于等于 1536 （或者十六进制的 0x0600）时，帧使用的是 Ethernet II 格式。以太网中大多数的数据帧使用的是 Ethernet II 格式。

![img](https://pic2.zhimg.com/80/v2-37b23e8384fa64f77503b9d548da5311_1440w.jpg)
