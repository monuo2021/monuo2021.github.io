---
layout: port
title: 处理器性能公式
date: 2021-08-13 14:47:13
tags:
categories: 计算机体系结构
mathjax: true
description: 处理器性能分析及Amdal定律
---

### CPU 性能及其度量因素

CPU 性能度量的基本指标是**CPU 执行时间**。而几乎所有发的计算机都有一个以固定频率运行的时钟，称为**时钟周期**，或脉冲周期等。所以，我们可以利用时钟周期来表示 CPU 时间：

$$
程序CPU执行时间 = 程序CPU时钟周期数 × 时钟周期长度
$$

其中，时钟周期长度与时钟频率互为倒数，所以该公式也可表示为：

$$
程序CPU执行时间 =  \frac {程序CPU时钟周期数}{时钟频率}
$$

### 指令性能

除了时钟周期外，我们还需注意**指令数**，因为计算机必须通过执行指令来运行程序。

$$
CPU时钟周期 = 程序指令数 × 指令平均时钟周期数
$$

> **指令平均时钟周期数（CPI）**：表示执行某个程序或者程序片段时每条指令所需的时钟周期平均数。

### 经典的 CPU 性能公式

结合公式（1）、（2）、（3），我们可以得出**经典的 CPU 性能公式**：

$$
CPU时间 = 指令数 × CPI × 时钟周期长度
$$

或

$$
CPU时间 = \frac{指令数 × CPI}{时钟频率}
$$

### 影响处理器性能的因素

从该公式可以看出，**处理器性能取决于三个特性：时钟周期、CPI、指令数**。

- 时钟周期：硬件技术与组成
- CPI：组成与指令集体系结构
- 指令数：指令集体系结构与编译器技术

#### 如何测量这些因素

首先，通过运行程序来测量 CPU 时间，并且计算机说明书上通常介绍了时钟周期长度。

难以测量的时指令数与 CPI。

可以使用体系结构仿真器预先执行程序来测量执行的指令数，或者用大多数处理器中的硬件计数器来测量执行的指令数、平均 CPI 和性能损失。

但是，CPI 与计算机的各种设计细节密切相关（如存储系统、处理器结构、应用程序中不同类型的指令所占的比例），对于不同的应用程序它是不同的。

### Amdal 定律

Amdal 定律阐述了**对于特定改进的性能提升可能由所使用的改进特征的数量所限制**。也就是说，当提升系统的某部分性能时，对整个系统性能的影响取决于:

1. 这一部分有多重要(ɑ)
2. 这一部分性能提升了多少(k)

$$
加速比S = \frac{1}{(1-ɑ)+\frac{ɑ}{k}}
$$

> ɑ 是升级模块占比，k 是该部分性能提升比例
