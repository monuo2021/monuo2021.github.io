---
title: 处理器流水线重点
date: 2021-07-18 19:49:09
tags:
categories: 计算机体系结构
description: 跟着《自己动手写CPU》敲了一个MIPS处理器后，又陆续看了蜂鸟E200、NutShell处理器，对处理器设计有了一些了解。对处理器设计的重点有了一些把握。
---

## 一、处理器流水线概述

计算机体系结构中最经典的就是五级流水线，即**取值**、**译码**、**执行**、**访存**、**写回**五个步骤。

流水线本质上可以理解为**以面积换性能、以空间换时间**的手段。

## 二、取指

取值阶段的核心是“**快**”和“**连续不断**”。

### 2.1 快速取值

要快速从存储器中取出指令，首先要保证存储器的读延迟越小越好。为了使处理器可以以最快的速度取值，通常使用**ITCM**和**ICache**的方法。

- #### ITCM

  指令紧耦合存储器，指配置一段较小容量的存储器来存储指令，在物理上接近处理器核且专属处理器核。

  ITCM大大提升了**时距不相邻而空距相邻**的代码段的执行性能，且需要人为地把代码组织成空距相邻。如蜂鸟E200处理器

- #### ICache

  指令缓存。

  ICache智能地、自动地将**时距相邻而空距很远**的代码段放置在体内，CPU以单周期访问这些代码，大大提升了程序性能。如NutShell处理器。

### 2.2 连续不断

#### 处理非对齐指令

1. 普通指令非对齐

   剩余缓存

2. 分支跳转指令非对齐

   多体交叉存储器

#### 处理分支指令——分支预测

对于条件跳转/分支指令，理论上要在执行阶段完成之后，才能解析出最终的跳转结果，而这样会造成流水线浪费。利用分支预测，不仅可以使取值连续不断，同时可以解决流水线里**控制冲突**问题。

- ##### 静态预测

  1. 总预测分支指令不会发生跳转
  2. BTFN预测，对向后的跳转预测为跳，对向前的跳转预测为不跳。蜂鸟E200处理器采取该方法。

- ##### 动态预测

  利用已经执行过的指令的历史信息和分支本身的信息综合进行方向预测。

  例如NutShell处理器，通过综合考虑分支目标缓冲器 (Branch Target Buffer, **BTB**)、模式历史表 (Pattern History Table,  **PHT**) 和返回地址栈 (Return Address Stack, **RAS**) 对不同类型的转移指令做出快速的预测. 

  ![img](https://gitee.com/tanliang-TL/blogpic/raw/master/img/BPU-NutShell.jpg)

  BTB 中缓存了跳转指令的地址高位信息、跳转目标、指令类型等信息. 在做分支预测时会用取指 PC 索引 BTB 表项, 如果 PC 高位与读到的 BTB 表项的标签匹配则认为 BTB 命中, 再根据 BTB 中记录的指令类型判断跳转方向和跳转目标. 如果类型为条件分支指令,  则需要访问模式历史表 (PHT) 来判断是否跳转; 如果类型为返回指令, 则选择返回地址栈 (RAS) 的栈顶内容作为跳转目标;  如果类型为直接或间接跳转指令, 则选择 BTB 中记录的跳转目标.

  ![img](https://gitee.com/tanliang-TL/blogpic/raw/master/img/BTB-NutShell.jpg)

  

  

  ![sp20210718_152102_053](https://gitee.com/tanliang-TL/blogpic/raw/master/img/sp20210718_152102_053.png)

  当分支预测器判断一条指令为条件分支跳转时, 需要访问 PHT 进一步对跳转方向做出预测. PHT 的每一项都是一个**两位饱和计数器**, 计数器的内容指示了强跳转、弱跳转、弱不跳转、强不跳转四种分支指令可能所处的状态, 该两位计数器的高位则指示了分支预测的方向.

  ![](https://gitee.com/tanliang-TL/blogpic/raw/master/img/PHT-NutShell.jpg)

  两位饱和计数器是用于预测分支“方向”；BTB是用于预测分支“地址”。

  更详细的介绍可见[**分支预测器设计**](https://hitsz-cslab.gitee.io/arch/lab2/1-theory/)

## 三、执行

### 3.1 流水线数据冲突

执行阶段除了根据指令的具体类型进行运算外，另一个重要职能就是维护并解决流水线冲突。

- **WAR（Write After Read）**

  假设指令j是在指令i后面执行的指令，WAR表示指令i读出数据后，指令j才能写这个寄存器。

- **WAW（Write After Write）**

  假设指令j是在指令i后面执行的指令，WAW表示指令i将数据写入寄存器后，指令j才能将数据写入这个寄存器。

- **RAW（Read After Write）**

  假设指令j是在指令i后面执行的指令，RAW表示指令i将数据写入寄存器后，指令j才能从这个寄存器读取数据。

  ```
  ADD r1,r2,r3
  SUB r4,r5,r1     指令需要读取上一条指令的结果
  ```

解决数据冲突的方法：

- **寄存器重命名（Tomasulo算法）**

  顺序执行的指令写寄存器实在写回阶段，后续的指令也是在写回阶段写寄存器，所以不存在WAW依赖。读寄存器实在译码阶段进行，而写寄存器实在写回阶段，所以不存在WAR依赖。而且，可以通过寄存器重命名的方法解决WAW、WAR，无需担心其执行顺序。

- **数据前递（旁路）**

  ![img](https://img-blog.csdnimg.cn/20190401202913338.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3NpbmF0XzM2OTQ1NTky,size_16,color_FFFFFF,t_70)

  openmips使用该方法解决数据冒险问题。

- **流水线暂停**（stall）

  ![img](https://img-blog.csdnimg.cn/20190401195112319.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3NpbmF0XzM2OTQ1NTky,size_16,color_FFFFFF,t_70)

  蜂鸟E200使用了OITF模块，每一次派遣一个长指令，则会在OITF中分配一个表项(Entry)，在这个表项中会存储该长指令的结果寄存器索引。在流水线的写回(Write-back)点，每次按顺序写回一个长指令之后，就会将此指令在OITF中的表项移除。每条指令派遣时，都会将本指令的源操作数和目的操作数寄存器索引和OITF中的各个表项进行比对，从而判断本指令是否与已经被派遣出，且尚未写回的长指令产生RAW和WAW相关性。如果产生相关性，则stall住当前指令的派遣。如果没有RAW和WAW相关性，且该指令为多周期长指令，把该指令写入OITF，如果OITF是full，则仍要stall住管线，等待OITF释放空间后，再写入并派遣。

- **保留站**
- **乱序发射队列**

### 3.2 发射和派遣

派遣往往表示指令经过译码之后被派发到不同的运算单元的等待队列中的过程。

发射往往表示指令从运算单元的等待队列中（解决了数据依赖性之后）发射到运算单元开始执行的过程。

- 顺序发射、顺序执行、顺序写回

- 顺序发射、乱序执行、顺序写回

  乱序执行指在指令执行阶段可以由不同的运算单元同时执行不同的指令，从而提高性能。同时，乱序执行处理器避免了在顺序执行处理器处理过程中指令由于运算数据未到位所造成的等待。

- 顺序发射、乱序执行、乱序写回

  有些处理器会配置Re-order Buffer（ROB），存储经由执行阶段处理后得到的结果，并有 ROB 把该结果按照顺序写回到寄存器文件。这是典型的乱序写回实现。

- 顺序派遣、乱序发射、乱序执行、乱序写回

## 四、交付

交付模块有三个概念：交付、取消、冲刷。

- **交付**指指令不再是预测执行状态，被判定为真正地在处理器中被执行。
- **取消**指该指令最终被判定取消。
- **冲刷**是将没有交付的后续指令全部取消掉。

影响指令交付的因素：

- 中断、异常、分支预测指令
- 条件码

交付模块可以在不同的流水线位置中完成：

- 在”执行“阶段进行

  在流水线执行阶段，理论上已经可以将分支预测指令的结果解析完成。如蜂鸟E200。

- 在”写回“阶段进行

  由于有的指令需要多个周期才能写回结果，并可能产生错误异常。如openmips。

  ![DE5DD5973939E9DCC65C1F64B0D2D9CB](https://gitee.com/tanliang-TL/blogpic/raw/master/img/DE5DD5973939E9DCC65C1F64B0D2D9CB.png)

- 在重排序交付队列（Re-Order Commit Queue）中进行

## 五、写回

最终写回仲裁、长指令写回仲裁、OITF

最终写回仲裁：长指令比单指令有更高优先级。如果没有长指令写回，单指令可以比更早位置的长指令写回。

长指令写回仲裁：根据OITF判断长指令的先后关系，严格控制长指令的写回顺序
